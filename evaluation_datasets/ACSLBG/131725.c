/*@
    logic integer cube_volume(integer side) = side * side * side;

    lemma cube_volume_nonlinear:
        \forall integer side; 0 <= side <= 10 ==> cube_volume(side) <= side * side * 1000;
*/

int main() {
    return 0;
}
