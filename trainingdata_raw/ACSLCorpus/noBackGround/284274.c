/*@
    requires \true;
    ensures ((x) * ((y) + (z)) == ((x) * (y)) + ((x) * (z)));
*/
void check_mul_is_distributive_add(int x, int y, int z) {
    //@ assert ((x) * ((y) + (z)) == ((x) * (y)) + ((x) * (z)));
}

/*@
    requires \true;
    ensures ((x) * ((y) + (z)) == ((x) * (y)) + ((x) * (z)));
*/
void check_mul_is_distributive_add_1(int x, int y, int z) {
    //@ assert ((x) * ((y) + (z)) == ((x) * (y)) + ((x) * (z)));
}

/*@
    requires \true;
    ensures ((x) * ((y) + (z)) == ((x) * (y)) + ((x) * (z)));
*/
void check_mul_is_distributive_add_2(int x, int y, int z) {
    //@ assert ((x) * ((y) + (z)) == ((x) * (y)) + ((x) * (z)));
}

/*@
    requires \true;
    ensures ((x) * ((y) + (z)) == ((x) * (y)) + ((x) * (z)));
*/
void check_mul_is_distributive_add_3(int x, int y, int z) {
    //@ assert ((x) * ((y) + (z)) == ((x) * (y)) + ((x) * (z)));
}

int main() {
    return 0;
}
