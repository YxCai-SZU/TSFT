/*@
requires a <= b;
requires b <= c;
ensures a <= c;
*/
void calc_example_dd(int a, int b, int c) {
    //@ assert ((a) <= (b));
    //@ assert (((c)) <= (c));
    //@ assert ((a) <= (c));
}

/*@
requires a <= b;
requires b <= c;
ensures a <= c;
*/
void calc_example_e(int a, int b, int c) {
    //@ assert ((a) <= (b));
    //@ assert (((c)) <= (c));
    //@ assert ((a) <= (c));
}

/*@
requires a <= b;
requires b <= c;
ensures a <= c;
*/
void calc_example_f(int a, int b, int c) {
    //@ assert ((a) <= (b));
    //@ assert (((c)) <= (c));
    //@ assert ((a) <= (c));
}

/*@
requires a <= b;
requires b <= c;
ensures a <= c;
*/
void calc_example_g(int a, int b, int c) {
    //@ assert ((a) <= (b));
    //@ assert (((c)) <= (c));
    //@ assert ((a) <= (c));
}

/*@
requires a <= b;
requires b <= c;
ensures a <= c;
*/
void calc_example_h(int a, int b, int c) {
    //@ assert ((a) <= (b));
    //@ assert (((c)) <= (c));
    //@ assert ((a) <= (c));
}

/*@
requires a <= b;
requires b <= c;
ensures a <= c;
*/
void calc_example_i(int a, int b, int c) {
    //@ assert ((a) <= (b));
    //@ assert (((c)) <= (c));
    //@ assert ((a) <= (c));
}

/*@
requires a <= b;
requires b <= c;
ensures a <= c;
*/
void calc_example_j(int a, int b, int c) {
    //@ assert ((a) <= (b));
    //@ assert (((c)) <= (c));
    //@ assert ((a) <= (c));
}

int main() {
    return 0;
}
