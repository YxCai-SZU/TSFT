#include <stdbool.h>

/*@
    requires ((2) == 2 &&
        \valid((a) + (0 .. 1)) &&
        (1 <= ((a)[0]) <= 100) &&
        (1 <= ((a)[1]) <= 100));
    requires ((2) == 2 &&
        \valid((b) + (0 .. 1)) &&
        (1 <= ((b)[0]) <= 100) &&
        (1 <= ((b)[1]) <= 100));
    ensures \result == (a[1] <= b[0] || a[0] >= b[1]);
*/
bool func(int *a, int *b)
{
    //@ assert (1 <= (a[0]) <= 100);
    //@ assert (1 <= (a[1]) <= 100);
    //@ assert (1 <= (b[0]) <= 100);
    //@ assert (1 <= (b[1]) <= 100);
    //@ assert ((a[1]) <= (b[0]) || (a[0]) >= (b[1])) <==> (a[1] <= b[0] || a[0] >= b[1]);
    
    return a[1] <= b[0] || a[0] >= b[1];
}
