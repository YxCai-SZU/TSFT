/*@
    logic integer multiply_int(integer x, integer y) = x * y;

    lemma multiply_nonzero:
        \forall integer x, y;
            x > 0 && y > 0 ==> multiply_int(x, y) > 0;
*/

int main() {
    return 0;
}
