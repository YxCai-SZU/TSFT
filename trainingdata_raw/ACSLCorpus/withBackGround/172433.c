/*@
    lemma calc_example_10: \forall integer x; x - 5 <= x;
    lemma another_calc_example: \forall integer y; y <= y + 3;
    lemma yet_another_calc_example: \forall integer z; z - 2 <= z;
    lemma more_steps_in_calc: \forall integer a; a <= a + 3;
    lemma diff_rel_in_chain: \forall integer x; x <= x + 2;
    lemma even_odd_difference: \forall integer x; x <= x + 3;
    lemma subtract_on_left: \forall integer x; x <= x + 3;
    lemma same_relations: \forall integer a; a <= a + 2;
    lemma adding_subtracting: \forall integer a; a <= a + 2;
    lemma both_on_left: \forall integer x; x <= x + 3;
    lemma confirm_relations: \forall integer a; a <= a + 2;
    lemma all_of_the_above: \forall integer x; x <= x + 3;
    lemma this_is_it: \forall integer x; x <= x + 3;
    lemma complex_calc_chain: \forall integer n; n >= 0 ==> n * 2 + 1 == 2 * n + 1;
    lemma calc_chain_complex: \forall integer n; n >= 0 ==> n * 3 + 2 == 3 * n + 2;
    lemma multi_step_complex: \forall integer n; n >= 0 ==> n * 2 + 3 == 2 * n + 3;
    lemma multiple_ints: \forall integer x, y; x + y <= x + y + 2;
    lemma proofs_in_between: \forall integer x, y; x + y <= x + y + 2;
    lemma complicated_calc_chain: \forall integer x, y; x + y <= x + y + 3;
    lemma show_relations: \forall integer x, y; x + y <= x + y + 3;
    lemma a_few_rel: \forall integer x, y; x + y <= x + y + 3;
*/

int main() {
    return 0;
}
