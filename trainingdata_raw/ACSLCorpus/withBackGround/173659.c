/*@
    lemma calc_example_5: \forall integer x, y; x > y ==> x - y > 0;
    lemma calc_example_6: \forall integer x, y, z; x > y && y > z ==> x - z > 0;
    lemma calc_example_7: \forall integer x, y, z, w; x > y && y > z && z > w ==> x - w > 0;
    lemma calc_example_8: \forall integer x, y; x > y ==> x - y >= 1;
    lemma calc_example_9: \forall integer x, y; x > y ==> x - y >= 1;
    lemma calc_example_10: \forall integer x, y; x > y ==> x - y >= 1;
    lemma calc_example_11: \forall integer x, y; x > y ==> x - y >= 1;
    lemma calc_example_12: \forall integer x, y; x > y ==> x - y >= 1;
    lemma calc_example_13: \forall integer x, y; x > y ==> x - y >= 1;
    lemma calc_example_14: \forall integer x, y; x > y ==> x - y >= 1;
    lemma calc_example_15: \forall integer x, y; x > y ==> x - y >= 1;
*/

int main() {
    return 0;
}
