/*@
    predicate le(integer x, integer y) = x <= y;

    lemma proof_example_9: \forall integer x, y; le(x, y) ==> le(x, y);
    lemma proof_example_10: \forall integer x, y; le(x, y) ==> le(x, y);
    lemma proof_example_11: \forall integer x, y; le(x, y) ==> le(x, y);
    lemma proof_example_12: \forall integer x, y; le(x, y) ==> le(x, y);
    lemma proof_example_13: \forall integer x, y; le(x, y) ==> le(x, y);
    lemma proof_example_14: \forall integer x, y; le(x, y) ==> le(x, y);
    lemma proof_example_15: \forall integer x, y; le(x, y) ==> le(x, y);
    lemma proof_example_16: \forall integer x, y; le(x, y) ==> le(x, y);
    lemma proof_example_17: \forall integer x, y; le(x, y) ==> le(x, y);
    lemma proof_example_18: \forall integer x, y; le(x, y) ==> le(x, y);
*/

int main() {
    return 0;
}
