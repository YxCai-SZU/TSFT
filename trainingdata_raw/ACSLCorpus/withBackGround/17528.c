/*@
    logic integer func(integer x, integer y) = x + y;

    lemma func_proof:
        \forall integer x, y;
            x >= 0 && y >= 0 && x + y <= 20000 ==>
            func(x, y) >= 0 && func(x, y) <= 20000;
*/

int main() {
    return 0;
}
