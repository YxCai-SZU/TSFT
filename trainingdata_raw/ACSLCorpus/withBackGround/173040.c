/*@
    logic integer subtract(integer x, integer y) = x - y;
    logic integer add(integer x, integer y) = x + y;

    lemma calc_example_subtract:
        \forall integer a, b;
            a > 0 && b > 0 ==> subtract(a, b) < a;

    lemma calc_example_add:
        \forall integer a, b;
            a > 0 && b > 0 ==> add(a, b) > a;
*/

int main()
{
    return 0;
}
