/*@
predicate p(integer x) = x > 0;

lemma div_example:
    \forall integer x; x > 0 ==> x / 2 < x;

lemma range_property:
    \forall integer x; 1 <= x < 100 ==> p(x);

lemma range_all:
    \forall integer x; 1 <= x < 100 ==> p(x);
*/

int main() {
    return 0;
}
