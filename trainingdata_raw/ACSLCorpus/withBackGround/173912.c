/*@
    predicate multiply_by_zero_lemma(integer x) = x * 0 == 0;
    lemma multiply_by_zero: \forall integer x; multiply_by_zero_lemma(x);
    
    predicate multiply_by_one_lemma(integer x) = x * 1 == x;
    lemma multiply_by_one: \forall integer x; multiply_by_one_lemma(x);
    
    predicate add_to_zero_lemma(integer x) = x + 0 == x;
    lemma add_to_zero: \forall integer x; add_to_zero_lemma(x);
    
    predicate add_zero_to_x_lemma(integer x) = 0 + x == x;
    lemma add_zero_to_x: \forall integer x; add_zero_to_x_lemma(x);
*/

int main()
{
    return 0;
}
