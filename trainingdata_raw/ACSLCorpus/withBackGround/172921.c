/*@
    lemma intermediate_example_2: \forall integer x, y; x == y - 2 ==> x <= y;
    lemma intermediate_example_3: \forall integer x, y; x == y - 2 ==> x <= y;
    lemma transitive_example_1: \forall integer a, b, c; a <= b && b <= c ==> a <= c;
    lemma transitive_example_2: \forall integer a, b, c; a <= b && b <= c ==> a <= c;
    lemma transitive_example_3: \forall integer a, b, c; a <= b && b <= c ==> a <= c;
    lemma example_4: \forall integer a, b, c; a <= b && b <= c ==> a <= c;
    lemma example_5: \forall integer a, b, c; a <= b && b <= c ==> a <= c;
    lemma example_6: \forall integer a, b, c; a <= b && b <= c ==> a <= c;
    lemma example_7: \forall integer a, b, c; a <= b && b <= c ==> a <= c;
*/

int main() {
    return 0;
}
