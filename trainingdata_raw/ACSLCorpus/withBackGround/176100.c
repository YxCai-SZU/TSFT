/*@
    lemma nonlinear_arithmetic_example_10:
        \forall integer a, b; a > 0 && b > 0 ==> a % b == a - (a / b) * b;

    lemma nonlinear_arithmetic_example_11:
        \forall integer a, b, c; a > 0 && b > 0 && c > 0 ==> 
            a % b == a - (a / b) * b && 
            b % c == b - (b / c) * c;

    lemma nonlinear_arithmetic_example_12:
        \forall integer a, b, c, d; a > 0 && b > 0 && c > 0 && d > 0 ==> 
            a % b == a - (a / b) * b && 
            b % c == b - (b / c) * c && 
            c % d == c - (c / d) * d;

    lemma nonlinear_arithmetic_example_13:
        \forall integer a, b; a > 0 && b > 0 ==> a % b == a - (a / b) * b;
*/

int main() {
    return 0;
}
