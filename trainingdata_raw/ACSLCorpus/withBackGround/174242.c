/*@
predicate is_prime(integer x) = x > 1;

lemma test_prime: \forall integer i; is_prime(i) ==> \true;

lemma test_not_prime: \forall integer i; !is_prime(i) ==> \true;
*/

int main() {
    return 0;
}
