/*@
    lemma calc_example_3: \forall integer a; a + 1 <= a + 2 <= a + 3;
    lemma example_multiple_relations: \forall integer x; x == x + 0 && x <= x + 1 && x < x + 2 && x <= x + 3;
    lemma example_chain_of_equality: \forall integer x; x == x + 0 && x <= x + 1 && x < x + 2 && x <= x + 3;
    lemma example_chain_of_inequality: \forall integer x; x <= x + 1 && x < x + 2 && x <= x + 3;
*/

int main() {
    return 0;
}
