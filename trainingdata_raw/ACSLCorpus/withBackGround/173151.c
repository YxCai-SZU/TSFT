/*@
predicate lt(integer x, integer y) = x < y;

lemma calc_example_4: \forall integer x, y; lt(x, y) ==> x + 1 < y + 2;
lemma calc_example_5: \forall integer x, y; lt(x, y) ==> x - 2 < y;
lemma calc_example_6: \forall integer x, y; lt(x, y) ==> x + 1 <= y + 2;
lemma calc_example_7: \forall integer x, y; lt(x, y) ==> x + 1 <= y + 2;
*/

int main() {
    return 0;
}
