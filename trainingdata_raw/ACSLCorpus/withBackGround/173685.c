/*@
    logic integer multiply(integer x, integer y) = x * y;

    lemma check_multiply:
        multiply(4, 5) == multiply(5, 4);

    lemma multiply_is_commutative:
        \forall integer x, y;
        -1000 <= x <= 1000 && -1000 <= y <= 1000 ==>
        multiply(x, y) == multiply(y, x);
*/

int main() {
    return 0;
}
