/*@
    lemma calc_example_8: \forall integer x, y; x == y ==> x * 2 == y * 2;
    lemma calc_example_9: \forall integer a, b; a <= b ==> a * 3 <= b * 3;
    lemma calc_example_10: \forall integer a, b; a <= b ==> a * 10 <= b * 10;
    lemma calc_example_11: \forall integer a, b; a <= b ==> a * 100 <= b * 100;
    lemma calc_example_12: \forall integer a, b; a <= b ==> a * 1000 <= b * 1000;
    lemma calc_example_13: \forall integer a, b; a <= b ==> a * 10000 <= b * 10000;
    lemma calc_example_14: \forall integer a, b; a <= b ==> a * 100000 <= b * 100000;
    lemma calc_example_15: \forall integer a, b; a <= b ==> a * 1000000 <= b * 1000000;
    lemma calc_example_16: \forall integer a, b; a <= b ==> a * 10000000 <= b * 10000000;
    lemma calc_example_17: \forall integer a, b; a <= b ==> a * 100000000 <= b * 100000000;
*/

int main() {
    return 0;
}
