/*@
    predicate leq(integer a, integer b) = a <= b;

    lemma proof_leading_to_calc:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_failing:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_having_duplicates:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_nested_loop:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_unroll_oncesuccess:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_unroll_twice:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_folding_unroll:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_folding_twice:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_replacing:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_shallow_folding:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_constant_terms:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_subtraction_nonlinear_arith:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_shallow_nesting:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_unroll_folding:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_nested_conditionals:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_withdrawal:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_taut:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_fibonacci:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_multiplication_isolation:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_shallow_folding_transitivity:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_different_status:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_conj_addition_deletion:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_max_unroll:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_associative_transitive:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_nesting:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_multiplication_isolation_add:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_max_depth:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_max_nothing:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_max_for_tupled:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_min_isolation:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_max_isolation:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_mins:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_max_4_args:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_max:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_max_6_args:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);

    lemma proof_max_7_args:
        \forall integer a, b, c;
            leq(a, b) && leq(b, c) ==> leq(a, c);
*/

int main() {
    return 0;
}
