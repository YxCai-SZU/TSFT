/*@
    lemma example_2: \forall integer a, b, c; a + b == c ==> a + b <= c + 1;
    lemma example_3: \forall integer a, b, c; a + b == c ==> a + b >= c - 1;
    lemma example_4: \forall integer a, b, c; a + b == c ==> a + b <= c + 1;
    lemma example_5: \forall integer a, b, c; a + b == c ==> a + b <= c + 1;
    lemma example_6: \forall integer a, b, c; a + b == c ==> a + b <= c + 1;
    lemma example_7: \forall integer a, b, c; a + b == c ==> a + b <= c + 1;
    lemma example_8: \forall integer a, b, c; a + b == c ==> a + b <= c + 1;
    lemma example_9: \forall integer a, b, c; a + b == c ==> a + b <= c + 1;
    lemma example_10: \forall integer a, b, c; a + b == c ==> a + b <= c + 1;
*/

int main() {
    return 0;
}
