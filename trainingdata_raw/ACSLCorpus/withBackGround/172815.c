/*@
    logic integer product(integer x, integer y) = x * y;

    lemma product_is_monotonic:
        \forall integer x, y;
            x >= 0 && y >= 0 && x <= 1 && y <= 1 ==>
            product(x, y) <= product(x + 1, y + 1);
*/

int main() {
    return 0;
}
