#include <stdbool.h>

/*@
    logic integer square(integer x) = x * x;

    lemma square_idem:
        \forall integer x;
            x >= -46340 && x <= 46340 ==> square(x) == x * x;
*/

int main()
{
    return 0;
}
