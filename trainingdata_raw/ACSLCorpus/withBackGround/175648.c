/*@
    predicate triple_inequality(integer x) = 3 * x >= x + x;
    predicate six_times_inequality(integer x) = 6 * x >= 2 * x;
    predicate eight_times_inequality(integer x) = 8 * x >= 2 * x;
    predicate nine_times_inequality(integer x) = 9 * x >= 2 * x;
    predicate ten_times_inequality(integer x) = 10 * x >= 2 * x;
    predicate eleven_times_inequality(integer x) = 11 * x >= 2 * x;
    predicate twelve_times_inequality(integer x) = 12 * x >= 2 * x;
    predicate thirteen_times_inequality(integer x) = 13 * x >= 2 * x;
    predicate fourteen_times_inequality(integer x) = 14 * x >= 2 * x;
    predicate fifteen_times_inequality(integer x) = 15 * x >= 2 * x;
    predicate sixteen_times_inequality(integer x) = 16 * x >= 2 * x;
    predicate seventeen_times_inequality(integer x) = 17 * x >= 2 * x;
    predicate eighteen_times_inequality(integer x) = 18 * x >= 2 * x;
    predicate nineteen_times_inequality(integer x) = 19 * x >= 2 * x;
    predicate twenty_times_inequality(integer x) = 20 * x >= 2 * x;
    predicate twenty_one_times_inequality(integer x) = 21 * x >= 2 * x;
    predicate twenty_two_times_inequality(integer x) = 22 * x >= 2 * x;
    predicate twenty_three_times_inequality(integer x) = 23 * x >= 2 * x;
    predicate twenty_four_times_inequality(integer x) = 24 * x >= 2 * x;
    predicate twenty_five_times_inequality(integer x) = 25 * x >= 2 * x;

    lemma triple_lemma: \forall integer x; x >= 0 ==> triple_inequality(x);
    lemma six_times_lemma: \forall integer x; x >= 0 ==> six_times_inequality(x);
    lemma eight_times_lemma: \forall integer x; x >= 0 ==> eight_times_inequality(x);
    lemma nine_times_lemma: \forall integer x; x >= 0 ==> nine_times_inequality(x);
    lemma ten_times_lemma: \forall integer x; x >= 0 ==> ten_times_inequality(x);
    lemma eleven_times_lemma: \forall integer x; x >= 0 ==> eleven_times_inequality(x);
    lemma twelve_times_lemma: \forall integer x; x >= 0 ==> twelve_times_inequality(x);
    lemma thirteen_times_lemma: \forall integer x; x >= 0 ==> thirteen_times_inequality(x);
    lemma fourteen_times_lemma: \forall integer x; x >= 0 ==> fourteen_times_inequality(x);
    lemma fifteen_times_lemma: \forall integer x; x >= 0 ==> fifteen_times_inequality(x);
    lemma sixteen_times_lemma: \forall integer x; x >= 0 ==> sixteen_times_inequality(x);
    lemma seventeen_times_lemma: \forall integer x; x >= 0 ==> seventeen_times_inequality(x);
    lemma eighteen_times_lemma: \forall integer x; x >= 0 ==> eighteen_times_inequality(x);
    lemma nineteen_times_lemma: \forall integer x; x >= 0 ==> nineteen_times_inequality(x);
    lemma twenty_times_lemma: \forall integer x; x >= 0 ==> twenty_times_inequality(x);
    lemma twenty_one_times_lemma: \forall integer x; x >= 0 ==> twenty_one_times_inequality(x);
    lemma twenty_two_times_lemma: \forall integer x; x >= 0 ==> twenty_two_times_inequality(x);
    lemma twenty_three_times_lemma: \forall integer x; x >= 0 ==> twenty_three_times_inequality(x);
    lemma twenty_four_times_lemma: \forall integer x; x >= 0 ==> twenty_four_times_inequality(x);
    lemma twenty_five_times_lemma: \forall integer x; x >= 0 ==> twenty_five_times_inequality(x);
*/

/*@ assigns \nothing; */
int main()
{
    return 0;
}
