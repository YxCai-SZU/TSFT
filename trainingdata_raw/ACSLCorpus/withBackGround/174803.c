/*@
    lemma is_transitive:
        \forall integer a, b, c; a < b && b < c ==> a < c;

    lemma calc_example_7:
        \forall integer a, b, c; a < b && b < c ==> a < c;

    lemma greater_than_or_equal_example:
        \forall integer x; x >= 0 ==> x >= 0;

    lemma less_than_example:
        \forall integer x; x < 10 ==> x < 10;

    lemma equivalence_example:
        \forall integer x, y; x == y ==> x == y;

    lemma less_than_or_equal_example:
        \forall integer x; x <= 5 ==> x <= 5;

    lemma greater_than_example:
        \forall integer x; x > 0 ==> x > 0;

    lemma not_equal_example:
        \forall integer x, y; x != y ==> x != y;

    lemma less_than_or_equal_example2:
        \forall integer x; x <= 5 ==> x <= 5;

    lemma greater_than_or_equal_example2:
        \forall integer x; x >= 0 ==> x >= 0;

    lemma equivalence_example2:
        \forall integer x, y; x == y ==> x == y;

    lemma less_than_example2:
        \forall integer x; x < 10 ==> x < 10;

    lemma greater_than_example2:
        \forall integer x; x > 0 ==> x > 0;

    lemma not_equal_example2:
        \forall integer x, y; x != y ==> x != y;
*/

int main() {
    return 0;
}
