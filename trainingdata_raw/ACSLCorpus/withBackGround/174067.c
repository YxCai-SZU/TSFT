#include <stdbool.h>

/*@
    lemma non_linear_arith_example14:
        \forall integer a, b;
            a > 0 && b > 0 ==> a * b != 0;

    lemma non_linear_arith_example15:
        \forall integer a, b;
            a >= 0 && b >= 0 && a <= 100 && b <= 100 ==> a * b <= 10000;

    lemma non_linear_arith_example16:
        \forall integer a, b;
            a > 0 && b > 0 && a <= 100 && b <= 100 ==> a * b > 0;

    lemma non_linear_arith_example17:
        \forall integer a, b;
            a > 0 && b > 0 && a <= 100 && b <= 100 ==> a * b <= 10000;

    lemma non_linear_arith_example18:
        \forall integer a, b;
            a > 0 && b > 0 && a <= 100 && b <= 100 ==> a * b > 0;

    lemma non_linear_arith_example19:
        \forall integer a, b;
            a >= 0 && b >= 0 && a <= 100 && b <= 100 ==> a * b <= 10000;

    lemma non_linear_arith_example20:
        \forall integer a, b;
            a >= 0 && b >= 0 && a <= 100 && b <= 100 ==> a * b <= 10000;

    lemma non_linear_arith_example21:
        \forall integer a, b;
            a > 0 && b > 0 && a <= 100 && b <= 100 ==> a * b > 0;

    lemma non_linear_arith_example22:
        \forall integer a, b;
            a > 0 && b > 0 && a <= 100 && b <= 100 ==> a * b > 0;

    lemma non_linear_arith_example23:
        \forall integer a, b;
            a >= 0 && b >= 0 && a <= 100 && b <= 100 ==> a * b <= 10000;

    lemma non_linear_arith_example24:
        \forall integer a, b;
            a >= 0 && b >= 0 && a <= 100 && b <= 100 ==> a * b <= 10000;

    lemma non_linear_arith_example25:
        \forall integer a, b;
            a > 0 && b > 0 && a <= 100 && b <= 100 ==> a * b > 0;
*/

int main() {
    return 0;
}
