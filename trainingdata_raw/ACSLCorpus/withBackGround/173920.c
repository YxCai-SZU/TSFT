/*@
    predicate pos(integer x) = x > 0;

    lemma calc_example_5:
        \forall integer y; pos(y) ==> y * 2 >= y;

    lemma calc_example_6:
        \forall integer a; pos(a) ==> a * 3 >= a;

    lemma calc_example_7:
        \forall integer x; pos(x) ==> x * 4 >= x;
*/

int main() {
    return 0;
}
