/*@
    predicate le(integer a, integer b) = a <= b;

    lemma calc_example_10: \forall integer x, y; le(x, y) ==> -y <= -x;
    lemma calc_example_11: \forall integer a, b; le(a, b) ==> a + 3 <= b + 3;
    lemma calc_example_12: \forall integer a, b; le(a, b) ==> 3 <= 5;
    lemma calc_example_13: \forall integer x, y; le(x, y) ==> x - 3 <= y - 3;
    lemma calc_example_14: \forall integer x, y; le(x, y) ==> 3 * x <= 3 * y;
    lemma calc_example_15: \forall integer x, y; le(x, y) ==> 2 * x <= 2 * y;
*/

int main() {
    return 0;
}
