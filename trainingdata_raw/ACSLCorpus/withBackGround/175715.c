/*@
    predicate in_range(integer x) = 1 <= x <= 100;

    lemma calc_example_7:
        \forall integer a, b; a <= b ==> a * 2 <= b * 2;

    lemma lemma1:
        \forall integer x; in_range(x) ==> 1 <= x * 2 <= 200;

    lemma lemma2:
        \forall integer x; in_range(x) ==> 1 <= x * 3 <= 300;

    lemma lemma3:
        \forall integer x; in_range(x) ==> 1 <= x * 4 <= 400;

    lemma lemma4:
        \forall integer x; in_range(x) ==> 1 <= x * 5 <= 500;

    lemma lemma5:
        \forall integer x; in_range(x) ==> 1 <= x * 6 <= 600;

    lemma lemma6:
        \forall integer x; in_range(x) ==> 1 <= x * 7 <= 700;

    lemma lemma7:
        \forall integer x; in_range(x) ==> 1 <= x * 8 <= 800;

    lemma lemma8:
        \forall integer x; in_range(x) ==> 1 <= x * 9 <= 900;

    lemma lemma9:
        \forall integer x; in_range(x) ==> 1 <= x * 10 <= 1000;
*/

int main() {
    return 0;
}
