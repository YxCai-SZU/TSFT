/*@
    lemma calc_example_11: \forall integer n; n >= 0 ==> n * 2 + 3 >= 3;
    lemma calc_example_12: \forall integer n, m; n + 1 == m ==> 2 * n + 2 == 2 * m;
    lemma calc_example_13: \forall integer a, b, c; a < b && b < c ==> a < c;
    lemma calc_example_14: \forall integer a, b, c; a <= b && b <= c ==> a <= c;
    lemma calc_example_15: \forall integer a, b, c; a <= b && b <= c && c <= a + 2 ==> a <= c;
    lemma calc_example_16: \forall integer n, m; n + 1 == m && n >= 0 ==> n * 2 + 2 == 2 * m;
    lemma calc_example_17: \forall integer n; n >= 0 ==> n * 2 + 3 >= 3;
    lemma calc_example_18: \forall integer n; n >= 0 ==> n * 2 + 4 >= 4;
    lemma calc_example_19: \forall integer n; n >= 0 ==> n * 2 + 5 >= 5;
    lemma calc_example_20: \forall integer n, m; n + 1 == m && n >= 0 ==> n * 2 + 2 == 2 * m;
*/

int main() {
    return 0;
}
