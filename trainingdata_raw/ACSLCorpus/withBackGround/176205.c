/*@
    logic integer sum_of_squares_and_triangle(integer n) =
        n <= 0 ? 0 : n * n + (n * (n + 1)) / 2;

    lemma sum_of_squares_and_triangle_nonneg:
        \forall integer n; n >= 0 ==> sum_of_squares_and_triangle(n) >= 0;
*/

int main() {
    return 0;
}
