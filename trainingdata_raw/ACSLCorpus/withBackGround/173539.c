/*@
    logic integer mod_value(integer x, integer y) = x % y;

    lemma broadcast_mod_value_in_range:
        \forall integer x, y; x >= 0 && y > 0 ==> mod_value(x, y) >= 0;
*/

int main() {
    return 0;
}
