/*@
    lemma non_linear_arith:
        \forall integer a, b, c;
        0 <= a <= 5 && 0 <= b <= 5 && 0 <= c <= 5 ==>
        a * (b + c) <= a * b + a * c;
    
    lemma non_linear_arith_2:
        \forall integer a, b, c;
        0 <= a <= 5 && 0 <= b <= 5 && 0 <= c <= 5 ==>
        a * (b + c) <= a * b + a * c;
    
    lemma non_linear_arith_3:
        \forall integer a, b, c;
        0 <= a <= 5 && 0 <= b <= 5 && 0 <= c <= 5 ==>
        a * (b + c) <= a * b + a * c;
*/

int main() {
    //@ assert \true;
    return 0;
}
